{
  "_comment": [
    "REFERENCE ONLY — Claude Code does not auto-discover this file.",
    "To activate, add the hook config below to ~/.claude/settings.json",
    "under the top-level 'hooks' key. See install.sh for instructions."
  ],
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "node ~/.claude/skills/schliff/hooks/session-injector.js"
      }
    ]
  }
}
