# Internal drafts and marketing (not for public repo)
docs/specs/show-hn-draft.md
docs/specs/viral-growth-*
demo/twitter/
docs/launch/posts/
docs/launch/awesome-claude-code-*
docs/specs/plans/v7.1-worktree-plan.json

# Regenerable launch corpus (re-fetchable via scripts/launch/collect_corpus.py)
# License of individual third-party files varies — not bundled under Schliff's MIT.
docs/launch/corpus/

# Personal planning/session-handoff artifacts
.paul/

# Schliff working files (generated during runs, not committed)
schliff-results.tsv
schliff-eval-results.json
schliff-report.md
schliff-workspace/
.schliff-eval/
.schliff/
judge_v0_results.jsonl

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code session artifacts (hooks spawn agents in worktrees here)
.claude/worktree-*.json
.claude/worktrees/
# Local-only Claude Code settings (personal harness/plugin toggles)
.claude/settings.json
.claude/settings.local.json

# Test artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# Environment
.env
.env.local

# Build
dist/
build/
*.egg-info/

# Local uv dev lockfile — schliff ships as a library via pyproject (no dep pinning)
uv.lock

# Playwright MCP session artifacts
.playwright-mcp/

# LLM-council session artifacts (verdict + full transcript). These carry
# adoption numbers, private decision context and advisor reasoning — they are
# working notes, not repo content.
council-report-*.html
council-transcript-*.md
